PORT=5001
# Auth URLs are set per-environment in docker-compose files:
# - docker-compose.dev.yaml: http://localhost:5173 (Vite)
# - docker-compose.prod.yaml: http://localhost (nginx)
# - K8s ConfigMaps: http://weather-agent.com (Ingress)